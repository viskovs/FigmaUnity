{
  "name": "figma-gamedev",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node --check code.js && node --test tests/*.test.js"
  }
}
